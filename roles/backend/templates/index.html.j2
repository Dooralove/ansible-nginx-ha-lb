<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Backend {{ inventory_hostname }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f6f8;
            text-align: center;
            padding-top: 50px;
        }
        .box {
            display: inline-block;
            padding: 30px 50px;
            background: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }
        h1 {
            color: #333;
        }
        p {
            font-size: 18px;
            color: #555;
        }
        .host {
            font-weight: bold;
            color: #007acc;
        }
    </style>
</head>
<body>
    <div class="box">
        <h1>Hello from backend!</h1>
        <p>You are served by <span class="host">{{ inventory_hostname }}</span></p>
        <p>Server IP: <span class="host">{{ ansible_host | default(inventory_hostname) }}</span></p>
        <p>Deployed at: {{ ansible_date_time.date }} {{ ansible_date_time.time }}</p>
    </div>
</body>
</html>
